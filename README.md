Uses the React Context API to leverage the `amazon-cognito-identity-js`
library for your React app. See `examples` folder for ... examples.


#### Dependencies
- `react-router` v4 or greater
- `react` v16 or greater